langcode: en
status: true
dependencies:
  config:
    - search_api.server.database
  module:
    - search_api
id: content_entities
name: 'Content Entities'
description: 'All content entities that can be viewed.'
read_only: false
field_settings:
  label:
    label: Label
    property_path: aggregated_field
    type: text
    configuration:
      type: first
      fields: []
processor_settings:
  add_url:
    plugin_id: add_url
    settings: {  }
  aggregated_field:
    plugin_id: aggregated_field
    settings: {  }
  rendered_item:
    plugin_id: rendered_item
    settings: {  }
options:
  index_directly: false
  cron_limit: 50
datasource_settings: {}
tracker_settings:
  default:
    plugin_id: default
    settings: {  }
server: database
